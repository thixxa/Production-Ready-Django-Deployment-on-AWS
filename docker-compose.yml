services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SECRET_KEY=local-docker-secret-change-me
      - DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
      - DJANGO_USE_HTTPS=false
